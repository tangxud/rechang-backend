# ===== 本地开发环境 =====
# 中间件由工作区根目录 docker-compose.dev.yml 提供（MySQL/Redis 初始数据与密码与这里对应）

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rechang?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      # 与 docker-compose.dev.yml 的 REDIS_PASSWORD 默认值保持一致，改一处需同步另一处
      password: rechang_dev_2026

# 本地开发直接用内置默认密钥（与其保持一致以便文档示例）
jwt:
  secret: rechang-secret-key-for-jwt-token-2026-must-be-at-least-32-chars
qr:
  hmac-secret: rechang-qr-code-hmac-secret-key-2026

logging:
  level:
    com.rechang: debug
    org.springframework.security: info
